export type PolicySection = { title: string; body: string[]; bullets?: string[] };
export function PolicyPage({ title, summary, sections, review = true }: { title: string; summary: string; sections: PolicySection[]; review?: boolean }) { return <main><section className="policy-hero shell"><p className="eyebrow">Governance</p><h1>{title}</h1><p className="lede">{summary}</p>{review && <div className="review-note">Draft for legal and operational review before publication.</div>}</section><article className="policy-body shell">{sections.map(section => <section key={section.title}><h2>{section.title}</h2>{section.body.map((p, i) => <p key={i}>{p}</p>)}{section.bullets && <ul>{section.bullets.map(x => <li key={x}>{x}</li>)}</ul>}</section>)}</article></main>; }
